<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.newsrec.manage.mapper.CommentsMapper">

    <resultMap type="Comments" id="CommentsResult">
        <result property="commentId"    column="comment_id"    />
        <result property="userId"    column="user_id"    />
        <result property="newsId"    column="news_id"    />
        <result property="content"    column="content"    />
        <result property="remark"    column="remark"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateTime"    column="update_time"    />
        <result property="createBy"    column="create_by"    />
        <result property="updateBy"    column="update_by"    />
    </resultMap>

    <sql id="selectCommentsVo">
        select comment_id, user_id, news_id, content, remark, create_time, update_time, create_by, update_by from tb_comments
    </sql>

    <select id="selectCommentsList" parameterType="Comments" resultMap="CommentsResult">
        <include refid="selectCommentsVo"/>
        <where>
            <if test="userId != null "> and user_id = #{userId}</if>
            <if test="newsId != null "> and news_id = #{newsId}</if>
            <if test="content != null  and content != ''"> and content like concat('%', #{content}, '%')</if>
        </where>
    </select>

    <select id="selectCommentsByCommentId" parameterType="Long" resultMap="CommentsResult">
        <include refid="selectCommentsVo"/>
        where comment_id = #{commentId}
    </select>

<!--  Vo查询语句  -->
    <select id="selectCommentsVoList" resultType="com.newsrec.manage.domain.vo.CommentsVo">
        select c.*, u.username,n.title,u.avatar from tb_comments c
        left join tb_users u on c.user_id = u.user_id
        left join tb_news n on c.news_id = n.news_id
        <where>
            <if test="userId != null "> and c.user_id = #{userId}</if>
            <if test="newsId != null "> and c.news_id = #{newsId}</if>
            <if test="username != null and username != ''"> and u.username like concat('%', #{username}, '%')</if>
            <if test="title != null and title != ''"> and n.title like concat('%', #{title}, '%')</if>
            <if test="content != null  and content != ''"> and content like concat('%', #{content}, '%')</if>
        </where>
        ORDER BY c.comment_id DESC
    </select>

    <insert id="insertComments" parameterType="Comments" useGeneratedKeys="true" keyProperty="commentId">
        insert into tb_comments
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="userId != null">user_id,</if>
            <if test="newsId != null">news_id,</if>
            <if test="content != null and content != ''">content,</if>
            <if test="remark != null">remark,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateTime != null">update_time,</if>
            <if test="createBy != null">create_by,</if>
            <if test="updateBy != null">update_by,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="userId != null">#{userId},</if>
            <if test="newsId != null">#{newsId},</if>
            <if test="content != null and content != ''">#{content},</if>
            <if test="remark != null">#{remark},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateTime != null">#{updateTime},</if>
            <if test="createBy != null">#{createBy},</if>
            <if test="updateBy != null">#{updateBy},</if>
         </trim>
    </insert>

    <update id="updateComments" parameterType="Comments">
        update tb_comments
        <trim prefix="SET" suffixOverrides=",">
            <if test="userId != null">user_id = #{userId},</if>
            <if test="newsId != null">news_id = #{newsId},</if>
            <if test="content != null and content != ''">content = #{content},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="createBy != null">create_by = #{createBy},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
        </trim>
        where comment_id = #{commentId}
    </update>

    <delete id="deleteCommentsByCommentId" parameterType="Long">
        delete from tb_comments where comment_id = #{commentId}
    </delete>

    <delete id="deleteCommentsByCommentIds" parameterType="String">
        delete from tb_comments where comment_id in
        <foreach item="commentId" collection="array" open="(" separator="," close=")">
            #{commentId}
        </foreach>
    </delete>
</mapper>
